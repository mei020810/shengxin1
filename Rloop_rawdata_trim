# md5sum -c md5.txt 
#示例：-q 25 --stringency 1 --fastqc --length 35 --paired --trim1 原始reads 先用trim_galore 过滤
# -q 25：质量控制;  --stringency 1：接头序列与读长序列匹配时所需的最小重叠碱基数;  --fastqc：质控报告;  --length 35：长度过滤，读长的长度小于35个碱基，则整条读长都会被丢弃;  --paired：双端模式 --trim1:从每个read的3'端修剪1个碱基
#利用bowtie1比对
#示例：bowtie -a -m 1 --best --strata -p 80 --chunkmbs 200  -X 800 ~/index/Gmax_508_v4.0.bw1  -1 $sample'_R1_val_1.fq.gz'  -2 $sample'_R2_val_2.fq.gz'  -S $sample'_x800.bw1.sam'
# -a : 报告所有可比对结果; -m 1 : 限制每条序列最多1个可比对位置;  --best --strata : 报告最佳比对;  -p 80 : 使用80个CPU核心;  -X 800 : 建库时超声波打断的最大插入片段大小800bp
# https://www.jianshu.com/p/e20a3b73dcd0 比对完用picard 去重复
# 分离wR-loop和cR-loop：perl -anle 'print if (($F[1]==99)||($F[1]==147))'  $sample'_x800.bw1.sam'  > $sample'_x800.bw1.cRloop.sam' ; perl -anle 'print if (($F[1]==83)||($F[1]==163))'  $sample'_x800.bw1.sam'  > $sample'_x800.bw1.wRloop.sam'
#perl 是 Perl 语言解释器(linux默认安装)，-anle 是 Perl 命令行选项组合，核心用于 “逐行分割文本→按列筛选→规范输出”；$F[1] 对应 SAM 的第 2 列（FLAG 值）；将筛选后的行保存到目标 SAM 文件
# 进行 峰calling ：macs2 callpeak -t $sample'_x800.bw1.sam'  --extsize 200 -q 0.05 --nomodel --SPMR -f SAM -g 1e+9 -B -n $sample'_x800.bw1.nomodel.macs2'；macs2 callpeak -t $sample'_x800.bw1.wRloop.sam'  --extsize 200 -q 0.05 --nomodel --SPMR -f SAM -g 1e+9 -B -n $sample'_x800.bw1.wRloop.nomodel.macs2'；macs2 callpeak -t $sample'_x800.bw1.cRloop.sam'  --extsize 200 -q 0.05 --nomodel --SPMR -f SAM -g 1e+9 -B -n $sample'_x800.bw1.cRloop.nomodel.macs2'
# macs3参数相同，-t输入文件，--extsize 需匹配建库时的插入片段长度（可用picard的CollectInsertSizeMetrics统计）200为通用值；-q是峰的显著性阈值-q 0.05表示仅保留假阳性率＜5% 的峰；nomodel不构建模型，手动指定--extsize时必用；--spmr输出“每百万reads的信号强度”，消除不同样本测序的差异；-f SAM明确输入文件为sam格式；-g指定参考基因组总长度；-B输出 bigWig 格式的信号轨道文件，用于 IGV 可视化；-n指定所有输出文件的前缀

conda activate study_rna
vim trim_rloop.sh
#!/bin/bash

# 设置最大并发任务数
MAX_JOBS=2
current_jobs=0

for r1_file in *_R1.fastq.gz; do
    r2_file=${r1_file/_R1/_R2}
    sample_name=${r1_file%%_R1.fastq.gz}
    
    # 如果当前任务数达到上限，等待
    while [ $(jobs -r | wc -l) -ge $MAX_JOBS ]; do
        echo "等待空闲资源... 当前运行任务: $(jobs -r | wc -l)"
        sleep 60
    done
    
    echo "启动: $sample_name - $(date)"
    
    # 启动任务
    nohup trim_galore --cores 4 -q 25 --stringency 1 --fastqc --length 35 --paired \
        --three_prime_clip_R1 1 --three_prime_clip_R2 1 \  #代替--trim1
        --output_dir ./trimmed_results \
        $r1_file $r2_file > ${sample_name}_trim.log 2>&1 &

    current_jobs=$((current_jobs + 1))
    echo "进度: $current_jobs 个任务已提交"
    
    sleep 5
done

echo "所有任务提交完成，等待最终完成..."
wait
echo "全部任务完成！时间: $(date)"

chmod u+x trim_rloop.sh
nohup ./trim_rloop.sh &  #tail -f nohup.out 查看任务进程 

cp /mnt/nfs2/stu_fy/hms/Data/Gmax_508_Wm82/Gmax_508_v4.0.fa /data/mshe/rloopData
# 建立bwtie的索引
bowtie-build -f Gmax_508_v4.0.fa ./index/Gmax_508_v4.0.bw1     #bw1
bowtie2-build --threads 10 Gmax_508_v4.0.fa ./index/Gmax_508_v4.0.bw2     #bw2

# 创建解压目录（bowtie1只能用解压后的.fq文件）
mkdir trimmed_results_uncompressed
# 批量解压所有文件
for file in trimmed_results/*.fq.gz; do
    base=$(basename "$file" .gz)
    echo "解压: $file"
    gunzip -c "$file" > "trimmed_results_uncompressed/$base"
done

# 比对
vim bw1_rloop.sh

#!/bin/bash
# 设置最大并发任务数
MAX_JOBS=2
current_jobs=0
INDEX_BASE="/data/mshe/rloopData/index/Gmax_508_v4.0.bw1"

for r1_file in trimmed_results_uncompressed/*_R1_val_1.fq; do
    # 提取样本名（去掉路径和_R1_val_1.fq）
    sample=$(basename "$r1_file" _R1_val_1.fq)
    
    # 检查对应的R2文件是否存在
    r2_file="trimmed_results_uncompressed/${sample}_R2_val_2.fq"
    if [ ! -f "$r2_file" ]; then
        echo "警告: 找不到对应的R2文件 $r2_file，跳过"
        continue
    fi
    
    while [ $(jobs -r | wc -l) -ge $MAX_JOBS ]; do
        echo "等待空闲资源... 当前运行任务: $(jobs -r | wc -l)"
        sleep 60
    done
    echo "启动: $sample - $(date)"

 nohup bowtie -a -m 1 --best --strata -p 10 --chunkmbs 200 -X 800 \
        "$INDEX_BASE" \
        -1 "$r1_file" \
        -2 "$r2_file" \
        -S "/data/mshe/rloopData/sam/${sample}_x800.bw1.sam" > "/data/mshe/rloopData/sam/${sample}_bw1.log" 2>&1 &
    
    current_jobs=$((current_jobs + 1))
    echo "进度: $current_jobs 个任务已提交"
    
    sleep 5
done 
echo "所有任务提交完成，等待最终完成..."
wait
echo "全部任务完成！时间: $(date)"

chmod u+x bw1_rloop.sh
nohup ./bw1_rloop.sh > "bw1_rloop.log" 2>&1 &
# 提取所有样本的比对率进行对比
for log_file in sam/*_bw1.log; do
    sample=$(basename "$log_file" _bw1.log)
    alignment_rate=$(grep "reads with at least one" "$log_file" | awk '{print $10}' | tr -d '()%')
    total_reads=$(grep "reads processed" "$log_file" | awk '{print $4}')
    echo "$sample | 总序列: $total_reads | 比对率: ${alignment_rate}%"
done | sort -t'|' -k5 -nr
# bw1比对率过低，尝试bw2比对
conda activate study_rna
vim bw2_rloop.sh
#!/bin/bash
# 设置最大并发任务数
MAX_JOBS=2
current_jobs=0
INDEX_BASE="/data/mshe/rloopData/index/Gmax_508_v4.0.bw2"

echo "=== 开始Bowtie2比对分析 ==="
echo "开始时间: $(date)"
echo "索引: $INDEX_BASE"
echo "最大并发任务: $MAX_JOBS"

for r1_file in trimmed_results/*_R1_val_1.fq.gz; do
    # 提取样本名（去掉路径和_R1_val_1.fq.gz）
    sample=$(basename "$r1_file" _R1_val_1.fq.gz)
    
    # 检查对应的R2文件是否存在
    r2_file="trimmed_results/${sample}_R2_val_2.fq.gz"
    if [ ! -f "$r2_file" ]; then
        echo "警告: 找不到对应的R2文件 $r2_file，跳过"
        continue
    fi
    
    while [ $(jobs -r | wc -l) -ge $MAX_JOBS ]; do
        echo "等待空闲资源... 当前运行任务: $(jobs -r | wc -l)"
        sleep 60
    done
    echo "启动: $sample - $(date)"

nohup bowtie2 --end-to-end --no-discordant --no-mixed \
        -p 8 \
        -X 800 \
        -x "$INDEX_BASE" \
        -1 "$r1_file" \
        -2 "$r2_file" \
        -S "/data/mshe/rloopData/sam/${sample}_x800.bw2.sam" > "/data/mshe/rloopData/sam/${sample}_bw2.log" 2>&1 &

    current_jobs=$((current_jobs + 1))
    echo "进度: $current_jobs 个任务已提交"
    
    sleep 5
done 
echo "所有任务提交完成，等待最终完成..."
wait

echo "=== 最终统计结果 ==="

for log_file in /data/mshe/rloopData/sam/*_bw2.log; do
    if [ -f "$log_file" ]; then
        sample=$(basename "$log_file" _bw2.log)
        alignment_rate=$(grep "overall alignment rate" "$log_file" | awk '{print $1}' | tr -d '%')
        total_reads=$(grep "reads; of these:" "$log_file" | awk '{print $1}')
        
        if [ -n "$alignment_rate" ] && [ -n "$total_reads" ]; then
            echo "$sample | 总序列: $total_reads | 比对率: ${alignment_rate}%"
        else
            echo "$sample | 统计信息提取失败"
        fi
    fi
done | sort -t'|' -k5 -nr

echo "全部任务完成！时间: $(date)"

chmod u+x bw2_rloop.sh
nohup ./bw2_rloop.sh > "bw2_rloop.log" 2>&1 &    # tail -f bw2_rloop.log



# sam → bam
vim sam_to_bam_picard.sh
#!/bin/bash
MAX_JOBS=2
current_jobs=0
# 创建输出目录
mkdir -p /data/mshe/rloopData/bam
mkdir -p /data/mshe/rloopData/picard
mkdir -p /data/mshe/rloopData/logs

echo "=== 开始批量处理 SAM 文件 ==="
echo "开始时间: $(date)"

for sam_file in sam/*.bw2.sam; do
    # 等待资源
    while [ $(jobs -r | wc -l) -ge $MAX_JOBS ]; do
        echo "等待空闲资源... 当前运行任务: $(jobs -r | wc -l)"
        sleep 60
    done
    
    sample=$(basename "$sam_file" _x800.bw2.sam)
    echo "启动处理: $sample - $(date)"
    
    # 在子shell中运行整个处理流程
    nohup bash -c "
        echo '  - SAM转BAM并排序: $sample'
        samtools sort -@ 8 '$sam_file' -o '/data/mshe/rloopData/bam/${sample}_x800.sorted.bam'

        echo '  - 建立索引: $sample'
        samtools index '/data/mshe/rloopData/bam/${sample}_x800.sorted.bam'

        echo '  - Picard标记重复: $sample'
        picard MarkDuplicates \
            REMOVE_DUPLICATES=true \
            CREATE_INDEX=true \
            MAX_FILE_HANDLES=800 \
            VALIDATION_STRINGENCY=LENIENT \
            I='/data/mshe/rloopData/bam/${sample}_x800.sorted.bam' \
            O='/data/mshe/rloopData/picard/${sample}_picard.bam' \  #如果没加_x800可以在这里加上
            M='/data/mshe/rloopData/picard/${sample}_picard_metrics.txt'  #如果没加_x800可以在这里加上
        
        echo '  - 完成: $sample'
    " > "/data/mshe/rloopData/logs/${sample}_sam_bam_picard.log" 2>&1 &
    
    current_jobs=$((current_jobs + 1))
    echo "进度: $current_jobs 个任务已提交 - 样本: $sample"
    sleep 10
done 

echo "所有任务提交完成，等待最终完成..."
wait
echo "=== 全部任务完成！ ==="
echo "完成时间: $(date)"
echo "总处理样本数: $current_jobs"

chmod u+x sam_to_bam_picard.sh
nohup ./sam_to_bam_picard.sh > sam_to_bam_picard.log 2>&1 &

# 利用samtools分析比对率(去重前)
vim samtools_flagstat_analysis.sh
#!/bin/bash
mkdir -p /data/mshe/rloopData/alignment_stats
echo "=== 开始BAM文件统计分析 ==="
echo "开始时间: $(date)"
for bam_file in /data/mshe/rloopData/bam/*.sorted.bam; do
    sample=$(basename "$bam_file" _x800.sorted.bam)
    echo "=== 分析 $sample ==="
samtools flagstat "$bam_file" > "/data/mshe/rloopData/alignment_stats/${sample}_flagstat.txt"
# 提取关键信息
    total_reads=$(grep "in total" "/data/mshe/rloopData/alignment_stats/${sample}_flagstat.txt" | awk '{print $1}')
    mapped_rate=$(grep "mapped" "/data/mshe/rloopData/alignment_stats/${sample}_flagstat.txt" | awk '{print $5}' | head -1)
    properly_paired=$(grep "properly paired" "/data/mshe/rloopData/alignment_stats/${sample}_flagstat.txt" | awk '{print $6}')
    if [ -n "$total_reads" ] && [ -n "$mapped_rate" ]; then
        echo "$sample: 总reads=$total_reads, 比对率=$mapped_rate, 正确配对=$properly_paired"
    else
        echo "$sample: 统计信息提取失败"
    fi
done
echo "=== 分析完成 ==="
echo "结束时间: $(date)"

chmod u+x samtools_flagstat_analysis.sh
nohup ./samtools_flagstat_analysis.sh > samtools_flagstat_analysis.log  2>&1 &

# 利用samtools去重后查看比对率
vim samtools_flagstat_analysis_picard.sh
#!/bin/bash
mkdir -p /data/mshe/rloopData/picard/alignment_stats
echo "=== 开始BAM文件统计分析 ==="
echo "开始时间: $(date)"
for bam_file in /data/mshe/rloopData/picard/*_picard.bam; do
    sample=$(basename "$bam_file" _picard.bam)
    echo "=== 分析 $sample ==="
samtools flagstat "$bam_file" > "/data/mshe/rloopData/picard/alignment_stats/${sample}_flagstat_picard.txt"
# 提取关键信息
    total_reads=$(grep "in total" "/data/mshe/rloopData/picard/alignment_stats/${sample}_flagstat_picard.txt" | awk '{print $1}')
    mapped_rate=$(grep "mapped" "/data/mshe/rloopData/picard/alignment_stats/${sample}_flagstat_picard.txt" | awk '{print $5}' | head -1)
    properly_paired=$(grep "properly paired" "/data/mshe/rloopData/picard/alignment_stats/${sample}_flagstat_picard.txt" | awk '{print $6}')
    if [ -n "$total_reads" ] && [ -n "$mapped_rate" ]; then
        echo "$sample: 总reads=$total_reads, 比对率=$mapped_rate, 正确配对=$properly_paired"
    else
        echo "$sample: 统计信息提取失败"
    fi
done
echo "=== 分析完成 ==="
echo "结束时间: $(date)"

chmod u+x samtools_flagstat_analysis_picard.sh
nohup ./samtools_flagstat_analysis_picard.sh > samtools_flagstat_analysis_picard.log  2>&1 &

# 比较去重前后的比对率
echo "样本 | 去重前比对率 | 去重后比对率 | 总reads减少"
echo "------|-------------|-------------|------------"

for stats_file in /data/mshe/rloopData/picard/alignment_stats/*_flagstat_picard.txt; do
    sample=$(basename "$stats_file" _flagstat_picard.txt)
    
    # 去重后数据
    post_total=$(grep "in total" "$stats_file" | awk '{print $1}')
    post_mapped=$(grep "mapped" "$stats_file" | awk '{print $5}' | head -1)
    # 去重前数据
    pre_file="/data/mshe/rloopData/alignment_stats/${sample}_flagstat.txt"
    if [ -f "$pre_file" ]; then
        pre_total=$(grep "in total" "$pre_file" | awk '{print $1}')
        pre_mapped=$(grep "mapped" "$pre_file" | awk '{print $5}' | head -1)
        reduction=$(echo "scale=1; (1 - $post_total / $pre_total) * 100" | bc)
        echo "$sample | $pre_mapped | $post_mapped | ${reduction}%"
    else
        echo "$sample | - | $post_mapped | -"
    fi
done

# 用bw区分wR-loop和cR-loop,注意不要重复添加x800标记
vim strand_specific.sh

#!/bin/bash
mkdir -p /data/mshe/rloopData/strand_specific
echo "=== 开始链特异性筛选 ==="
echo "开始时间: $(date)"

for bam_path in /data/mshe/rloopData/picard/*_picard.bam; do
    sample=$(basename "$bam_path" _picard.bam)

        echo "开始处理: $sample"
        echo "BAM文件: $bam_path"
        
    echo "cRloop筛选..."
        samtools view -h "$bam_path" | \
        perl -anle 'print if (($F[1]==99)||($F[1]==147))' > "/data/mshe/rloopData/strand_specific/${sample}_x800_cRloop.sam"
#（防止表头被删）perl -anle 'print if (/^@/ || ($F[1]==99)||($F[1]==147))' > "/data/mshe/rloopData/strand_specific/${sample}_x800_cRloop.sam"
    
echo "wRloop筛选..."
        samtools view -h "$bam_path" | \
        perl -anle 'print if (($F[1]==83)||($F[1]==163))' > "/data/mshe/rloopData/strand_specific/${sample}_x800_wRloop.sam"
#perl -anle 'print if (/^@/ || ($F[1]==83)||($F[1]==163))' > "/data/mshe/rloopData/strand_specific/${sample}_x800_wRloop.sam"

        # 统计筛选结果
        cRloop_count=$(grep -c '^[^@]' "/data/mshe/rloopData/strand_specific/${sample}_x800_cRloop.sam")
        wRloop_count=$(grep -c '^[^@]' "/data/mshe/rloopData/strand_specific/${sample}_x800_wRloop.sam")
    echo "完成: $sample - cRloop: $cRloop_count reads, wRloop: $wRloop_count reads"
    echo "----------------------------------------"
done 
echo "全部任务完成！时间: $(date)"

chmod u+x strand_specific.sh
./strand_specific.sh

vim rename_files.sh
#!/bin/bash
echo "=== 安全批量重命名 ==="

# 1. Picard BAM文件
echo "--- 处理Picard BAM文件 ---"
for file in /data/mshe/rloopData/picard/*_picard.bam; do
    if [ -f "$file" ] && [[ "$file" != *"_x800_picard.bam" ]]; then
        new_file="${file/_picard.bam/_x800_picard.bam}"
        echo "$(basename "$file") -> $(basename "$new_file")"
        mv "$file" "$new_file"
    fi
done

# 2. cRloop文件
echo "--- 处理cRloop文件 ---"
for file in /data/mshe/rloopData/strand_specific/*_cRloop.sam; do
    if [ -f "$file" ] && [[ "$file" != *"_x800_cRloop.sam" ]]; then
        new_file="${file/_cRloop.sam/_x800_cRloop.sam}"
        echo "$(basename "$file") -> $(basename "$new_file")"
        mv "$file" "$new_file"
    fi
done

# 3. wRloop文件
echo "--- 处理wRloop文件 ---"
for file in /data/mshe/rloopData/strand_specific/*_wRloop.sam; do
    if [ -f "$file" ] && [[ "$file" != *"_x800_wRloop.sam" ]]; then
        new_file="${file/_wRloop.sam/_x800_wRloop.sam}"
        echo "$(basename "$file") -> $(basename "$new_file")"
        mv "$file" "$new_file"
    fi
done

echo "=== 重命名完成 ==="
chmod +x rename_files.sh
./rename_files.sh

#补全无表头的sam文件
for suffix in "_x800_cRloop.sam" "_x800_wRloop.sam"; do
    for sam_file in /data/mshe/rloopData/strand_specific/*${suffix}; do
        sample=$(basename "$sam_file" "${suffix}")
        raw_bam="/data/mshe/rloopData/picard/${sample}_x800_picard.bam"
        
        echo "文件: $(basename "$sam_file") -> 样本: $sample"
        
        if [ -f "$raw_bam" ]; then
            samtools view -H "$raw_bam" > header.tmp
            cat header.tmp "$sam_file" > "${sam_file}.tmp"
            mv "${sam_file}.tmp" "$sam_file"
            echo "已补全表头"
        else
            echo "找不到BAM: $raw_bam"
        fi
    done
done
# 删除临时文件
rm -f header.tmp

vim macs3_peak_calling.sh
#!/bin/bash
mkdir -p /data/mshe/rloopData/macs3_results_full
mkdir -p /data/mshe/rloopData/macs3_results_wRloop
mkdir -p /data/mshe/rloopData/macs3_results_cRloop

echo "=== 开始MACS3 Peak Calling ==="
echo "开始时间: $(date)"

# 1. 全数据peak calling
echo "1. 全数据peak calling..."
for bam_file in /data/mshe/rloopData/picard/*_x800_picard.bam; do
    sample=$(basename "$bam_file" _x800_picard.bam)
    # 检查是否已经运行过
    if [ -f "${output_prefix}_peaks.xls" ]; then
        echo "跳过已处理的样本：$sample"
        continue
    fi
    echo "处理样本：$sample"
    macs3 callpeak -t "$bam_file" \
    --extsize 200 -q 0.05 \
    --nomodel --SPMR -f BAM \
    -g 1e+9 -B \
    -n "/data/mshe/rloopData/macs3_results_full/${sample}_x800_bw2_nomodel.macs3"
done

echo "2. wRloop peak calling..."
for wRloop_file in /data/mshe/rloopData/strand_specific/*_x800_wRloop.sam; do
    sample=$(basename "$wRloop_file" _x800_wRloop.sam)
    # 检查是否已经运行过
    if [ -f "${output_prefix}_peaks.xls" ]; then
        echo "跳过已处理的样本：$sample"
        continue
    fi
    echo "处理样本：$sample"
    macs3 callpeak -t "$wRloop_file" \
    --extsize 200 -q 0.05 \
    --nomodel --SPMR -f SAM \
    -g 1e+9 -B \
    -n "/data/mshe/rloopData/macs3_results_wRloop/${sample}_x800_bw2_wRloop_nomodel.macs3"
done

echo "3. cRloop peak calling..."
for cRloop_file in /data/mshe/rloopData/strand_specific/*_x800_cRloop.sam; do
    sample=$(basename "$cRloop_file" _x800_cRloop.sam)
    # 检查是否已经运行过
    if [ -f "${output_prefix}_peaks.xls" ]; then
        echo "跳过已处理的样本：$sample"
        continue
    fi
    echo "处理样本：$sample"
    macs3 callpeak -t "$cRloop_file" \
    --extsize 200 -q 0.05 \
    --nomodel --SPMR -f SAM \
    -g 1e+9 -B \
    -n "/data/mshe/rloopData/macs3_results_cRloop/${sample}_x800_bw2_cRloop_nomodel.macs3"
done
echo "=== MACS3 Peak Calling 完成 ==="
echo "结束时间: $(date)"

chmod u+x macs3_peak_calling.sh
nohup ./macs3_peak_calling.sh > macs3_peak_calling.log 2>&1 &

#wRloop的sam文件加了表头还有格式问题，用samtools转换成bam文件再跑（转的过程会自动修复格式问题）
# 创建目录（如果不存在）
mkdir -p "/data/mshe/rloopData/temp_bam_wRloop"   # 文件夹已改名为bam_wRloop
for sam_file in /data/mshe/rloopData/strand_specific/*_x800_wRloop.sam; do
    sample=$(basename "$sam_file" _x800_wRloop.sam)
    bam_file="/data/mshe/rloopData/temp_bam_wRloop/${sample}_x800_wRloop.bam"
    sorted_bam="${bam_file%.bam}.sorted.bam"
    output_prefix="/data/mshe/rloopData/macs3_results_wRloop/${sample}_x800_bw2_wRloop_nomodel.macs3"
    echo "处理: $(basename "$sam_file")"
    # 检查MACS3输出是否已存在
    if [ -f "${output_prefix}_peaks.xls" ]; then
        echo "  MACS3结果已存在，跳过: ${sample}"
        continue
    fi
    # 检查BAM文件是否已存在
    if [ -f "$sorted_bam" ]; then
        echo "  BAM文件已存在，直接使用: $(basename "$sorted_bam")"
    else
        echo "  转换: $(basename "$sam_file") -> BAM格式"
        # 使用samtools转换
        samtools view -b -S "$sam_file" -o "$bam_file"
        # 排序和索引
        samtools sort "$bam_file" -o "$sorted_bam"
        samtools index "$sorted_bam"
        # 删除中间文件节省空间
        rm -f "$bam_file"
    fi
    echo "  使用BAM文件运行MACS3: $sample"
    macs3 callpeak -t "$sorted_bam" \
        --extsize 200 -q 0.05 \
        --nomodel --SPMR -f BAM \
        -g 1e+9 -B \
        -n "$output_prefix"
    
    echo "  完成: $sample"
    echo "---"
done

#cRloop的sam→bam 
mkdir -p "/data/mshe/rloopData/bam_cRloop"
for sam_file in /data/mshe/rloopData/strand_specific/*_x800_cRloop.sam; do
    sample=$(basename "$sam_file" _x800_cRloop.sam)
    bam_file="/data/mshe/rloopData/bam_cRloop/${sample}_x800_cRloop.bam"
    sorted_bam="${bam_file%.bam}.sorted.bam"
    echo "处理: $(basename "$sam_file")"
    # 检查BAM文件是否已存在
    if [ -f "$sorted_bam" ]; then
        echo "  BAM文件已存在"
    else
        echo "  转换: $(basename "$sam_file") -> BAM格式"
        # 使用samtools转换
        samtools view -b -S -@ 8 "$sam_file" -o "$bam_file"
        # 排序和索引
        samtools sort -@ 8 "$bam_file" -o "$sorted_bam"
        samtools index "$sorted_bam"
        # 删除中间文件节省空间
        rm -f "$bam_file"
    fi
done

#deeptools可视化需要bam的排序文件及其同名索引，此处给picard的索引改名
for file in /data/mshe/rloopData/picard/*_picard.bai; do
    if [ -f "$file" ] && [[ "$file" != *"_x800_picard.bai" ]]; then
        new_file="${file/_picard.bai/_x800_picard.bai}"
        echo "$(basename "$file") -> $(basename "$new_file")"
        mv "$file" "$new_file"
    fi
done
for file in /data/mshe/rloopData/picard/*_picard_metrics.txt; do
    if [ -f "$file" ] && [[ "$file" != *"_x800_picard_metrics.txt" ]]; then
        new_file="${file/_picard_metrics.txt/_x800_picard_metrics.txt}"
        echo "$(basename "$file") -> $(basename "$new_file")"
        mv "$file" "$new_file"
    fi
done

#deeptools识别bam文件容易出错，将所有样本转为bigwig文件
vim create_bigwig.sh

#!/bin/bash
mkdir -p /data/mshe/rloopData/deeptools/bigwigs
echo "开始时间: $(date)"

# 为所有样本生成bigWig文件
echo "=== 生成bigWig文件 ==="
# Full数据的BAM文件
for bam_file in /data/mshe/rloopData/picard/*_x800_picard.bam; do
    sample=$(basename "$bam_file" _x800_picard.bam)
    bw_file="/data/mshe/rloopData/deeptools/bigwigs/${sample}_x800_full.bw"
    
    if [ ! -f "$bw_file" ]; then
        echo "生成full bigWig: $sample"
        bamCoverage -b "$bam_file" -o "$bw_file" --binSize 10 --normalizeUsing RPKM --numberOfProcessors 4
    else
        echo "full bigwig文件已存在：$sample"
    fi
done

# wRloop数据的BAM文件
for bam_file in /data/mshe/rloopData/bam_wRloop/*_x800_wRloop.sorted.bam; do
    if [ ! -f "$bam_file" ]; then
        echo "文件不存在，跳过: $bam_file"
        continue
    fi
    sample=$(basename "$bam_file" _x800_wRloop.sorted.bam)
    bw_file="/data/mshe/rloopData/deeptools/bigwigs/${sample}_x800_wRloop.bw"
    
    if [ ! -f "$bw_file" ]; then
        echo "生成wRloop bigWig: $sample"
        bamCoverage -b "$bam_file" -o "$bw_file" --binSize 10 --normalizeUsing RPKM --numberOfProcessors 4
    else
        echo "wRloop bigWig已存在: $sample"
    fi
done

# cRloop数据的BAM文件
for bam_file in /data/mshe/rloopData/bam_cRloop/*_x800_cRloop.sorted.bam; do
    if [ ! -f "$bam_file" ]; then
        echo "文件不存在，跳过: $bam_file"
        continue
    fi
    sample=$(basename "$bam_file" _x800_cRloop.sorted.bam)
    bw_file="/data/mshe/rloopData/deeptools/bigwigs/${sample}_x800_cRloop.bw"
    
    if [ ! -f "$bw_file" ]; then
        echo "生成cRloop bigWig: $sample"
        bamCoverage -b "$bam_file" -o "$bw_file" --binSize 10 --normalizeUsing RPKM --numberOfProcessors 4
    else
        echo "cRloop bigWig已存在: $sample"
    fi
done
echo "完成时间: $(date)"

chmod u+x create_bigwig.sh
nohup ./create_bigwig.sh > create_bigwig.log 2>&1 &

# deeptools可视化（取summit区域上下游1kb）
vim summit_deeptools.sh

#!/bin/bash
mkdir -p /data/mshe/rloopData/deeptools/summit_matrix
echo "=== 开始Deeptools Summit可视化 ==="
echo "开始时间: $(date)"

# 生成summit中心区域的矩阵（full）
for bw_file in /data/mshe/rloopData/deeptools/bigwigs/*_x800_full.bw; do
    sample=$(basename "$bw_file" _x800_full.bw)
    summit_bed="/data/mshe/rloopData/macs3_results_full/${sample}_x800_bw2_nomodel.macs3_summits.bed"
    
    if [ -f "$summit_bed" ]; then
        echo "处理样本: $sample"
        # 以summit为中心，上下游各1kb生成热图
        computeMatrix reference-point \
            --referencePoint center \
            -b 1000 -a 1000 \
            -R "$summit_bed" \
            -S "$bw_file" \
            -p 8 \
            -o "/data/mshe/rloopData/deeptools/summit_matrix/${sample}_x800_summit_matrix.gz"
        
        # 绘制热图
        echo "  绘制热图..."
        plotHeatmap -m "/data/mshe/rloopData/deeptools/summit_matrix/${sample}_x800_summit_matrix.gz" \
            -o "/data/mshe/rloopData/deeptools/summit_matrix/${sample}_x800_summit_heatmap" \
            --outFileFormats pdf svg png \
            --colorMap viridis \
            --plotTitle "${sample} R-loop Summit 上下游1kb信号热图" \
            --refPointLabel "Peak Summit"
    echo "  完成: $sample"
    fi
done

echo "=== 处理wRloop和cRloop数据 ==="
for peak_type in wRloop cRloop; do
    for bw_file in /data/mshe/rloopData/deeptools/bigwigs/*_x800_${peak_type}.bw; do
        if [ ! -f "$bw_file" ]; then
            echo "跳过不存在的BAM文件: $bw_file"
            continue
        fi
        sample_base=$(basename "$bw_file" _x800_${peak_type}.bw)
        summit_bed="/data/mshe/rloopData/macs3_results_${peak_type}/${sample_base}_x800_bw2_${peak_type}_nomodel.macs3_summits.bed"
        if [ -f "$summit_bed" ]; then
            echo "处理${peak_type}: $sample_base"
            matrix_file="/data/mshe/rloopData/deeptools/summit_matrix/${sample_base}_x800_${peak_type}_summit_matrix.gz"
            computeMatrix reference-point \
                --referencePoint center \
                -b 1000 -a 1000 \
                -R "$summit_bed" \
                -S "$bw_file" \
                -p 8 \
                -o "$matrix_file"
            echo "  绘制热图..."
            plotHeatmap -m "$matrix_file" \
                -o "/data/mshe/rloopData/deeptools/summit_matrix/${sample_base}_x800_${peak_type}_summit_heatmap" \
                --outFileFormats pdf svg png \
                --colorMap viridis \
                --plotTitle "${sample_base} ${peak_type} Peak Summit 上下游1kb信号热图" \
                --refPointLabel "Peak Summit" \
                --samplesLabel "${sample_base}_${peak_type}"
        echo "  完成: ${sample_base}_${peak_type}"
        else
            echo "  跳过: 找不到summit文件 $summit_bed"
        fi
    done
done
echo "=== Deeptools Summit可视化完成 ==="
echo "结束时间: $(date)"

chmod u+x summit_deeptools.sh
./summit_deeptools.sh
